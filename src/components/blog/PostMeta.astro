---
import { formatDate } from "@/helpers";

interface Props {
    date: string;
}

const { date } = Astro.props;
---

<p class="text-sm">
    Ecrito el: <span class="text-coffee-600 font-bold">{formatDate(date)}</span>
</p>
